import m from 'mithril';
import {Window, Tab, Tabs, Btn, Sidebar} from '../ui';


/**
 * Componente que mostra janela com informações demográficas.
 */
export class Population {
    view () {
        return <Window>Populacao</Window>
    }
}