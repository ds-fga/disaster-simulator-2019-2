import m = require('mithril');
import {Window, Tab, Tabs, Btn, Sidebar} from '../ui';


/**
 * Componente para janela inicial.
 */
export class Initial {
    view () {
        return <Window>Tela Inicial</Window>;
    }
}