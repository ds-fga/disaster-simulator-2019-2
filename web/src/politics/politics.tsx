import m from 'mithril';
import {Window, Tab, Tabs, Btn, Sidebar} from '../ui';


/**
 * Componente que mostra janela de política
 */
export class Politics {
    view () {
        return <Window>Politica</Window>
    }
}