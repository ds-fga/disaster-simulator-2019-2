import m from 'mithril';
import {Window, Tab, Tabs, Btn, Sidebar} from '../ui';


/**
 * Componente para janela de economia.
 */
export class Economy {
    view () {
        return <Window>Economia</Window>
    }
}